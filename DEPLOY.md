# 云函数上传部署操作步骤（个人小程序专用）

> 适用项目：`yaya-workbench/`
> 个人主体小程序无法使用插件（OCR/同声传译），改用云函数 + 百度智能云实现 TTS / ASR / OCR。
> 三个云函数：`tts`（英语朗读 + 语音输入）、`ocr`（拍照识别）、`security`（内容安全，文本敏感词审核，fail-open）

---

## 〇、前置条件（一次性）

| 项目 | 状态 |
| --- | --- |
| 微信云开发已开通，环境 ID = `cloud1-d6g3kvfyuf44f11a6` | ✅ 已配在 `app.js` |
| 个人主体小程序 AppID = `wx44b8c212d362a20b` | ✅ 已配在 `project.config.json` |
| 百度智能云账号 + 已创建「语音技术」和「文字识别」应用，拿到 API Key / Secret Key | ⏳ 需要你操作 |
| `node_modules` 已装在云函数目录 | ⏳ 需要你操作 |

> `app.json` 已**未声明任何插件**（个人账号插件系统整体关闭，声明了反而编译失败）。

---

## 一、填百度 API Key

> 仓库里 `config.js` 已被 `.gitignore` 排除（含真实密钥，不入库）。请这样准备本地配置：

1. 复制模板：`cloudfunctions/tts/config.example.js` → 同目录 `config.js`（ocr 同理）
2. 把里面的 `YOUR_API_KEY` / `YOUR_SECRET_KEY` 占位符替换成你自己的真实 Key

- 获取 Key：https://console.bce.baidu.com/ → 人工智能 → 语音技术（TTS/ASR）/ 文字识别（OCR）创建应用
- 本地 `config.js`（真实密钥）会被 DevTools 正常上传到云端，不受影响。

> 没账号的话：访问 https://console.bce.baidu.com/ → 注册并完成实名 → 「人工智能」→「语音技术」创建应用（同样路径「文字识别」创建 OCR 应用）→ 在应用详情页能看到 AK 和 SK。

---

## 二、装依赖到底怎么弄？（新手最常卡这一步）

> **先说结论：你本地完全不用碰命令行，也不用装任何东西。**

微信云开发在「上传并部署」时，只要选「**云端安装依赖（不上传 node_modules）**」，**它会在腾讯云的服务器上自动帮你 `npm install`**（把 `package.json` 里声明的 `wx-server-sdk` 装好，云端自带，几乎不会失败）。你电脑上连 Node 都不用装。

### 为什么有些人说要本地 `npm install`？
那是另一种上传方式「**上传 node_modules**」才需要——但你**不要**选那个，麻烦且容易冲突。
我们只用「云端安装依赖」这一种，最省事、最稳。

### （可选，仅当云端安装失败时）本地装
如果你云端部署后报错说缺依赖，再回来本地装。在微信开发者工具顶部菜单 `视图(View) → 终端(Terminal)` 打开面板，依次：

```bash
cd cloudfunctions/tts && npm install
cd ../ocr && npm install
```

装完改选「上传并部署：所有文件（连同 node_modules）」重传即可。

> 正常情况下这一步**跳过**，直接进第三步。

---

## 三、上传并部署（关键步骤）

> 必须选「云端安装依赖」，**不要**选「上传 node_modules」（大、慢、易冲突）。

### 步骤 1：部署 tts

1. 在微信开发者工具的「文件树」中，找到 `cloudfunctions/tts/` 文件夹
2. **右键** 该文件夹
3. 选择「**上传并部署：云端安装依赖（不上传 node_modules）**」
4. 弹出对话框确认：
   - 环境选择：`cloud1-d6g3kvfyuf44f11a6`（你的环境）
   - 直接点「确定」上传
5. 等待 10~30 秒，控制台会提示「上传成功，正在云端安装依赖」

### 步骤 2：部署 ocr

1. 右键 `cloudfunctions/ocr/` 文件夹
2. 同样选择「**上传并部署：云端安装依赖（不上传 node_modules）**」
3. 确认环境 → 确定

### 步骤 2.5：部署 security（内容安全）

1. 右键 `cloudfunctions/security/` 文件夹
2. 同样选择「**上传并部署：云端安装依赖（不上传 node_modules）**」
3. 确认环境 → 确定
> 这个云函数做文本敏感词审核（fail-open：云端异常不阻断用户写库），用于待办 / 饮食 / 记账 / 心情 / 感受 / 旅行等自由文本输入。

### 步骤 3：验证部署完成

打开「云开发控制台」（开发者工具左上角「云开发」按钮）：

1. 左侧点「云函数」
2. 应能看到 `tts`、`ocr`、`security` 三个函数，状态显示为「**运行中**」或「部署成功」

---

## 四、云函数配置（基本不用动，看一眼就行）

### 4.1 「云函数默认公网出访」——找不到就对了，不用开

> 旧版云开发控制台有这个开关，但**新版（你现在的个人免费环境 `cloud1-xxx`）云函数默认就开了公网出访**，控制台里已经没有这个独立开关。
> 官方文档原文：*云函数创建成功后，默认只有公网访问权限，即运行时能访问暴露在公共网络中的资源*（百度 API 就是公网 HTTPS）。

所以：
- **你在「设置」→「其他设置」里找不到「云函数默认公网出访」是完全正常的，不用管它，直接跳过。**
- 如果以后在某些老环境界面里真看到了这个开关，保持默认「开启」即可（免费，本来就该开）。

> 只有一种情况需要你关心网络：你主动在百度侧设了「固定 IP 白名单」。但我们用的免费环境云函数出口 IP 不固定，所以**百度侧不要设 IP 白名单**（留空 = 不限制），否则会调不通。

### 4.2 百度智能云侧不要设 IP 白名单

1. 登录 https://console.bce.baidu.com/
2. 「语音技术」/「文字识别」应用 → 「API 管理」/「IP 白名单」
3. **保持空白（不限制 IP）即可**。个人免费云函数出口 IP 不固定，设了反而调不通。

---

## 五、编译运行测试

1. 点击「编译」按钮编译小程序
2. 验证三件事：
   - **英语模块**：点 🔊 → 应能听到英文朗读 ✅
   - **语音输入**：点 🎤 → 说话 → 松手 → 应自动填到输入框 ✅
   - **拍照识别**：饮食/旅游模块点「📷 拍照/相册识别」→ 选照片 → 应自动填入文字 ✅

---

## 六、常见错误排查

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 部署后云函数列表空白 | 部署目标环境选错了 | 确认环境 ID 是 `cloud1-d6g3kvfyuf44f11a6` |
| 调用报 `Cloud API isn't enabled` | 云开发没开 | 右上角「云开发」按钮开通 |
| 调用报 `Connection timed out` / `getaddrinfo ENOTFOUND` | 网络异常（极少，免费环境默认公网出访已开） | 多半是百度接口地址写错，或临时网络抖动，重试 |
| 调用报 `Invalid AK/SK` | 百度 Key 没改 / 改错 | 检查 `config.js` 里有没有漏字符 |
| 调用报 `module 'axios' not found` | 没装依赖 / 选了「上传 node_modules」 | 重新选「**云端安装依赖**」部署 |
| 调用报 `access_token invalid` | 百度 Key 没启用对应接口 | 在百度控制台确认接口已激活 |

---

## 七、百度需要充钱吗（费用说明）

**结论：个人日常使用基本不花钱，不用先充值，直接免费额度就能跑通。**

- **文字识别（OCR）**：个人实名用户，通用文字识别有**每日免费额度**（新账号通常每天有几百次免费调用）。你只是偶尔拍张饭单/行程识别，完全在免费范围。
- **语音技术（TTS 朗读 + ASR 语音输入）**：同样有**免费额度 / 免费 QPS**，轻度使用不触发计费。

> 只有当你量非常大（比如每天几千次调用）才可能超出免费额度进入按量计费，那时候才需要充钱，而且很便宜（几分钱到几毛钱的量级）。
> **建议**：先不充钱，按本指南部署完直接试用。真遇到「免费额度用尽」的报错再考虑充值。

## 八、操作清单

- [ ] 拿到百度 AK / SK（语音技术 + 文字识别）
- [ ] 替换 `cloudfunctions/tts/config.js`
- [ ] 替换 `cloudfunctions/ocr/config.js`
- [ ] （跳过）「云函数默认公网出访」新版已默认开，找不到就忽略
- [ ] 右键 tts → 上传并部署：云端安装依赖
- [ ] 右键 ocr → 上传并部署：云端安装依赖
- [ ] 右键 security → 上传并部署：云端安装依赖
- [ ] 编译验证英语朗读 / 语音输入 / 拍照识别 / 自由文本写入拦截

完成后告诉我「部署好了」，我陪你做一遍最终联调。
