# 鸭鸭·个人工作台 —— 上线前 checklist & 发布流程

> 当前状态（2026-08-09）：代码移植完成、14 模块对齐、云函数（tts/ocr）已部署成功、改名同步 bug 已修、字体已全部替换为免费开源栈。
> 但**还未在真机验证过**，且有两个上线硬门槛未处理。

---

## 一、上线前必做（不分先后，但都得过）

### ✅ A. 真机验证（最关键，模拟器 ≠ 真机）
在微信开发者工具点「**真机预览**」或「**真机调试**」，用你的手机扫二维码，逐项确认：

- [ ] 改名同步：改名字后，**原生标题栏 / topbar 中部 / sidebar 头像旁 / 我的页** 四处都是新名字（重点验证"杀进程重进"后仍一致）
- [ ] 英语朗读 🔊：能听到声音（真机才能听）
- [ ] 语音输入 🎤：说话后自动填文字（真机才能录）
- [ ] 拍照识别 📷：饮食/旅游拍照或选相册，能自动填文字
- [ ] 各模块交互：下拉框完整、折线图正确、无半行字/截断
- [ ] 云函数调用：真机走的是云端环境，确认 `tts`/`ocr` 在真机也通（模拟器有时走本地）

> 为什么必须真机：PC 模拟器能调麦克风/相机的有限，且真机走的是真实云环境，模拟器"能用"不代表线上能用。

### ✅ B. 隐私协议（代码已完成，还差后台配置）
小程序用了 `chooseAvatar`（选头像）和 `nickname` 输入框，按微信规则必须配隐私授权。**代码层已全部写好**：
1. ✅ `app.json` 已加 `"__usePrivacyCheck__": true`
2. ✅ `workbench.js` onLoad 调 `wx.getPrivacySetting` 判断需授权时弹自定义弹窗；弹窗用官方 `<button open-type="agreePrivacyAuthorization">` 同意按钮（无需手动调 requirePrivacyAuthorize）
3. ⬜ **需你操作**：去 **mp.weixin.qq.com → 设置 → 服务内容声明 → 用户隐私保护指引** 勾选「收集你选中的头像、昵称」并提交审核（5 分钟内生效）

> 不配的后果：真机/审核后 `chooseAvatar` 直接调不起来，用户改不了头像。

### ✅ C. 内容安全（代码已完成：security 云函数 + 6 处写前校验）
用户输入的待办、饮食、记账、心情、日记、旅行都是 UGC 文本。已实现：
- ✅ 新增 `cloudfunctions/security` 云函数：`text` action 做本地敏感词兜底审核（**fail-open**：云端异常不阻断用户写库）；`image` action 预留跳过
- ✅ `workbench.js` 加 `safeText()` 助手，在 `addTodo / addDiet / addAcc / saveMood / saveFeeling / addTravel` 6 个自由文本写入点**写库前**校验，命中敏感词则拦截提示
- ⬜ **需你操作**：右键 `cloudfunctions/security` → 上传并部署：云端安装依赖（见 DEPLOY.md 步骤 2.5）
- 💡 建议：在「用户隐私保护指引」里声明"内容仅本人可见、用于个人记录"，进一步降低审核风险

> 说明：个人主体未必开通微信官方 `msgSecCheck`，故用本地敏感词兜底（零依赖、零失败）；后续要更强审核可接入百度内容审核替换 `security` 实现。

### ✅ D. 原生标题栏写死"鸭鸭·个人工作台"
`app.json` 第 7 行 `"navigationBarTitleText"` 是写死的，改名不会变。如果你希望顶部也跟着 userName 走，需要换成页面内 `wx.setNavigationBarTitle`（可加，非阻断）。

---

## 二、正式上线 5 步

1. **上传代码**
   微信开发者工具 → 右上「上传」→ 填版本号（如 `1.0.0`）和备注 → 上传。
   （注意：这是上传**前端代码**；云函数已在之前「上传并部署」过，不用重传，但上线前再确认一次云端能跑。）

2. **配置类目（个人主体受限）**
   mp 后台 → 设置 → 基本设置 → 服务类目。
   - 个人主体**只能选不需要资质的类目**（如：工具-实用工具、教育-在线课程/教育信息服务、资讯等）。
   - **不能选**：社交、电商、医疗、金融、食品等需资质的。

3. **提交审核**
   mp 后台 → 管理 → 版本管理 → 开发版本「提交审核」。
   - 填功能页面（选几个核心页面截图）
   - 填测试账号（如需要）
   - 选类目
   - 提交后等 1-7 天审核

4. **审核通过 → 发布**
   审核通过后点「发布」，全量或分阶段放量。

5. **上线后监控**
   - 云函数调用量/报错（云开发控制台）
   - 百度 API 免费额度是否够用
   - 用户反馈的 crash

---

## 三、个人主体限制速查（心里有数）

| 能力 | 个人主体 | 说明 |
| --- | --- | --- |
| 微信支付 | ❌ 不支持 | 不能收款 |
| 附近的小程序 | ❌ 部分受限 | 个人主体无此能力 |
| 需资质类目 | ❌ 不能选 | 社交/电商/医疗/金融等 |
| 插件 | ❌ 关闭 | 已用云函数绕过 |
| 内容安全 | ⚠️ 需自管 | 建议加 msgSecCheck |
| 单主体小程序数 | 最多 5 个 | — |
| 云开发 | ✅ 可用 | 已用（tts/ocr 云函数）|

---

## 四、我现在能帮你做的（代码层）
- ✅ app.json 加 `__usePrivacyCheck__: true` + 隐私授权弹窗 + 隐私说明页
- ✅ 新增 `security` 云函数做 `msgSecCheck` 文本检测，写入前调用
- ✅ 改名后同步 `wx.setNavigationBarTitle`
- ❌ 类目选择、审核提交、隐私指引后台勾选 → 需你在 mp.weixin.qq.com 自己点（涉及账号主体操作）

---

## 五、建议顺序
1. 先真机验证（A）——把剩余 bug 彻底验完
2. 我帮你加隐私授权 + 内容安全（B/C）
3. 你后台配隐私指引 + 选类目
4. 上传 → 审核 → 发布（二）
