# 鸭鸭·个人工作台 —— 测试版（体验版）上线 Runbook

> 代码已推送到 GitHub：`https://github.com/WilliamYLC/-.git`（main 分支）。
> 微信小程序的「体验版 / 测试版」必须由**你本人在微信开发者工具里上传代码**生成（账号绑定 + 扫码登录，无法在远程环境自动完成）。
> 本文件给你一步一步照着点即可，约 10 分钟拿到体验版二维码。

---

## 〇、前置（一次性，若已做过可跳过）
- 微信开发者工具已安装并登录你的公众号/小程序账号（AppID = `wx44b8c212d362a20b`，个人主体）。
- 已开通微信云开发，环境 ID = `cloud1-d6g3kvfyuf44f44f11a6`（已配在 `app.js`）。
- 百度智能云 AK/SK 已填进本地 `cloudfunctions/tts/config.js` 与 `cloudfunctions/ocr/config.js`（真实密钥已本地保留，未入库）。

---

## 一、打开项目
1. 打开微信开发者工具 → 「导入项目」。
2. 目录选：`yaya-workbench/`（含 `project.config.json` 的那层）。
3. AppID 选 `wx44b8c212d362a20b`（或「测试号」仅本地调试用，但上线必须用真实 AppID）。
4. 后端服务选「微信云开发」→ 导入。

---

## 二、部署 4 个云函数（关键！否则登录/朗读/识别全挂）
在左侧文件树，**逐个**右键文件夹 → 「上传并部署：云端安装依赖（不上传 node_modules）」：
- `cloudfunctions/login`
- `cloudfunctions/tts`
- `cloudfunctions/ocr`
- `cloudfunctions/security`

> 部署后点左上角「云开发」→ 云函数，应看到 4 个函数状态「运行中」。

---

## 三、建 3 个数据库集合
「云开发」→ 数据库 → 新建集合（权限全选「仅创建者可读写」）：
- `records`
- `user_profile`
- `user_modules`

---

## 四、上传代码 → 生成体验版
1. 开发者工具右上角点「**上传**」。
2. 版本号填 `1.0.0-beta`，备注写「测试版」。
3. 上传成功后，去 **mp.weixin.qq.com** → 管理 → 版本管理 → 「**体验版**」会显示刚上传的版本。
4. 点「扫码体验」生成二维码，用手机微信扫码即可真机测试。

---

## 五、隐私合规（不配则真机改不了头像/昵称）
mp.weixin.qq.com → 设置 → 服务内容声明 → 用户隐私保护指引：
- 勾选「收集你选中的头像、昵称」并提交审核（几分钟生效）。
- 代码层已加 `__usePrivacyCheck__: true` + 授权弹窗，后台声明后即可正常调起。

---

## 六、真机验收清单（模拟器 ≠ 真机，务必扫二维码验）
- [ ] 改名同步：改名字后四处（原生标题栏 / topbar / sidebar / 我的页）一致，杀进程重进仍一致
- [ ] 英语朗读 🔊 能出声
- [ ] 语音输入 🎤 说话自动填字
- [ ] 拍照识别 📷 饮食/旅游拍照或选相册自动填字
- [ ] 打卡提示「今日打卡成功！继续加油鸭 🦆」
- [ ] 初始页渐变 = hero 卡片渐变（135° 四站紫粉）

---

## 七、已知未接通项（非阻断，后续再做）
- `pages/profile`（我的页）与 `pages/module-detail`（模块详情）已有文件但**尚未接线导航**，当前仅 `workbench` + `addons` 两个页面可达。需要时在 `app.json` 的 `pages` 注册并补 `wx.navigateTo`。
- 原生标题栏文字写死「鸭鸭 · 个人工作台」，不改名跟随（如需跟随用 `wx.setNavigationBarTitle`）。
- 内容安全用本地敏感词兜底（fail-open），个人主体未接微信官方 `msgSecCheck`（审核前建议评估是否接入）。
